// withCredentials([usernamePassword(credentialsId: 'harshikagupta23', passwordVariable: 'password', usernameVariable: 'username')]) {
//     // some block
// }

node {  
    stage('Build') { 
        sh 'node index.js'
    }
}